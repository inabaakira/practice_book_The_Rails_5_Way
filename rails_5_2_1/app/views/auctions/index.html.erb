<% items = Item.all %>
<% items.each do |item| %>
  <%= link_to item.description, auction_path(item) %>

  <%= link_to "Delete", auction_path(item), method: :delete %>

<% end  %>

<%= items[0].description  %>

<%= form_for "auction", url: auction_path(items[0]), html: {method: :patch} do |f| %>
  <%= f.text_field :description %>
  <%= f.submit "Submit" %>
<% end %>

<%= form_tag auctions_path do %>
<% end %>

<%= link_to "Click here to view all auctions", auctions_path %>

<%= link_to "Create a new item", new_auction_path %>

<%= link_to "Edit this item", edit_auction_url(items[0]) %>

<%= link_to "See all bids", auction_bids_path(items[0]) %>

<%= link_to "Delete this bid", auction_bid_path(1,3), method: :delete %>

<!-- 下記は model を作らないと動かない -->
<%# = link_to "Delete this bid", [1,3], method: :delete %>
